import { Stack } from "@chakra-ui/react";
import { AnimeSearch } from "../components";

export const HomePage = () => {
  return (
    <Stack w={"100%"} h={"100%"}>
      <AnimeSearch />
    </Stack>
  );
};
